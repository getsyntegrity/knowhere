# Deployment Assets

This directory contains deployment-related assets owned by this repository.

## Included Here

- `deploy/docker/`: Dockerfiles for the API and worker services
- `deploy/local-dev/`: Docker Compose assets for local development

See `deploy/local-dev/README.md` for local stack usage.
