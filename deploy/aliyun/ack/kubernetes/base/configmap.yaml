apiVersion: v1
kind: ConfigMap
metadata:
  name: knowhere-config
  namespace: knowhere
data:
  ENVIRONMENT: "${ENVIRONMENT}"
  LOG_LEVEL: "INFO"
  PYTHONUNBUFFERED: "1"
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"

