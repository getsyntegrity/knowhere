# Terraform Backend 配置文件 - 测试环境（阿里云）
# 使用方法：
#   terraform init -backend-config=backend-config.test

bucket = "knowhere-terraform-state-test"
prefix = "terraform.tfstate"
region = "cn-guangzhou"
encrypt = true

