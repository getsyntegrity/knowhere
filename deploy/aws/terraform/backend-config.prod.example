# Terraform Backend 配置文件 - 生产环境
# 使用方法：
#   terraform init -backend-config=backend-config.prod

bucket         = "knowhere-terraform-state-prod"
key            = "terraform.tfstate"
region         = "us-east-1"  # 根据实际环境设置
dynamodb_table = "knowhere-terraform-locks-prod"
encrypt        = true

