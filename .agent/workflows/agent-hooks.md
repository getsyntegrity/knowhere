---
description: run agent hooks pre-flight context check
---

// turbo-all

Run the agent hooks CLI to get pre-computed context:

```bash
python3 -m agent_hooks.cli --hooks /Users/wuchengke/Desktop/knowhereapi-main/.agent-hooks/hooks.yaml
```
